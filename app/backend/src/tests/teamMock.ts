import TeamModel from '../database/models/TeamModel';

const teamMock = [
  new TeamModel({ id: 1, teamName: 'Avaí/Kindermann' }),
  new TeamModel({ id: 2, teamName: 'Bahia' }),
  new TeamModel({ id: 3, teamName: 'Botafogo' }),
];

export default teamMock;